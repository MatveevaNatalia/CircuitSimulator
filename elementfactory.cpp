#include "elementfactory.h"

Element * ElementFactory::CreateElement(std::string code)
{
// If code starts with "R" return new Resistor
// If code starts with "I" return new Inductance
// e.t.c
    // if error return 0
    return 0;
}
